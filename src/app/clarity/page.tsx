import { pillarMetadata, PillarHub } from "../components/PillarHub";

export const metadata = pillarMetadata("clarity");

export default function ClarityPage() {
  return <PillarHub pillar="clarity" />;
}
