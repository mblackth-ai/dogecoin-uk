import { pillarMetadata, PillarHub } from "../components/PillarHub";

export const metadata = pillarMetadata("belonging");

export default function BelongingPage() {
  return <PillarHub pillar="belonging" />;
}
