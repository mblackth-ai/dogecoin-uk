/**
 * Additional batches for later heartbeats (pass-04+).
 * Merged conceptually — apply-expansion-pass reads expansion-batches.cjs;
 * keep growing that file OR append here and require both.
 */
module.exports.moreBatches = [];
